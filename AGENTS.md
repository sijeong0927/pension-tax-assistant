# AGENTS.md

이 문서는 저장소 전체에서 작업하는 자동화 에이전트와 기여자가 따라야 할 기본 규칙입니다.

## 프로젝트 목표

이 프로젝트는 총급여 구간과 금융계좌 보유 상태를 바탕으로 연금계좌 세액공제 여력을 진단하고, 공식 자료에 근거한 RAG 챗봇 상담으로 이어지는 핀테크 MVP입니다.

핵심 사용자 흐름은 다음과 같습니다.

1. 총급여 구간과 계좌 보유 상태 입력
2. 결정론적 계산 엔진으로 예상 세액공제 효과 산출
3. 맞춤 진단 리포트와 추천 질문 제공
4. RAG 챗봇으로 추가 조건과 예외 상황 안내

## 작업 전 확인

- 현재 브랜치와 `git status`를 먼저 확인합니다.
- 저장소 루트의 `README.md`, 관련 GitHub 이슈, 현재 구현 파일을 함께 읽습니다.
- 이슈의 요구사항과 실제 코드가 충돌하면 추측으로 범위를 넓히지 말고 차이를 명시합니다.
- 기존 사용자 변경사항과 다른 작업자의 변경사항을 덮어쓰지 않습니다.

## Git 및 PR 규칙

- `main`에 직접 기능을 개발하지 않습니다.
- 이슈 하나마다 새 브랜치 하나와 PR 하나를 사용합니다.
- 새 브랜치는 최신 `main`에서 생성합니다.
- 브랜치 이름은 `<이슈번호>-<영역>-<짧은설명>` 형식을 권장합니다. 예: `4-ai-rag`.
- 커밋은 한 가지 목적만 담고 Conventional Commits 형식을 권장합니다.
  - `feat:`
  - `fix:`
  - `docs:`
  - `test:`
  - `refactor:`
  - `chore:`
- PR 본문에 관련 이슈와 검증 결과를 적고, 완료되는 이슈는 `Closes #<번호>`로 연결합니다.
- PR이 병합된 뒤 작업 브랜치를 삭제합니다.
- GitHub 상태를 확인하지 않고 이슈나 PR을 완료·병합된 것으로 문서화하지 않습니다.
- 이슈·PR 상태와 `main` 코드가 다르면 두 상태를 모두 기록하고 저장소 관리자에게 정리를 요청합니다.

## 아키텍처 경계

- `app/main.py`: 앱 생성, CORS, 공통 미들웨어, 라우터 등록만 담당합니다.
- `app/api/v1/`: HTTP 요청·응답과 API 라우팅을 담당합니다.
- `app/services/`: 진단 계산과 RAG 같은 비즈니스 로직을 담당합니다.
- `app/models/`: Pydantic 요청·응답 모델과 DB 모델을 담당합니다.
- `app/core/`: 설정, DB, Vector DB, 공통 프롬프트를 담당합니다.
- `app/data/`: 검증된 지식베이스 원문 또는 가공 데이터를 저장합니다.
- `frontend/`: 진단, 리포트, 챗봇 UI와 API 연동을 담당합니다.

API 라우터에 계산식이나 LLM 호출 로직을 직접 작성하지 않습니다. 재사용 가능한 로직은 서비스 계층으로 분리합니다.

## 세법 및 금융 정확성

- 세액공제 계산은 LLM이 아니라 테스트 가능한 Python 함수로 처리합니다.
- RAG와 LLM은 공식 자료 검색, 조건 설명, 출처 제시에 사용합니다.
- 세법 정보는 국세청, 국가법령정보센터, 기획재정부, 금융위원회 등 공식 1차 출처로 검증합니다.
- AI가 생성한 문장, 블로그, 커뮤니티 글은 단독 근거로 사용하지 않습니다.
- 지식 데이터에는 가능한 범위에서 다음 메타데이터를 포함합니다.
  - `source_title`
  - `source_url`
  - `effective_date`
  - `last_verified`
- “공제대상 납입액”, “예상 세액공제액”, “최종 환급액”을 구분합니다.
- 지방소득세 포함 여부와 계산 기준연도를 표시합니다.
- 확정할 수 없는 예외 조건은 단정하지 않고 공식 채널 또는 세무 전문가 확인을 안내합니다.
- 계산 결과에는 결정세액과 다른 공제 항목에 따라 실제 환급액이 달라질 수 있다는 면책을 유지합니다.

## RAG 구현 규칙

- FAQ JSON을 임베딩하기 전에 스키마, 중복, 출처, 기준일을 검증합니다.
- 검색 결과가 부족하거나 관련성이 낮으면 모델이 답을 만들어내지 않도록 합니다.
- 답변에는 사용한 근거 문서와 기준일을 반환할 수 있어야 합니다.
- OpenAI 모델명, API 키, ChromaDB 저장 경로는 환경변수 또는 설정 모듈에서 관리합니다.
- API 키와 개인 데이터는 코드, 테스트 픽스처, 로그, PR 본문에 넣지 않습니다.
- 생성된 Vector DB 파일과 로컬 DB 파일은 필요 시 `.gitignore`에 추가합니다.

## API 및 코드 규칙

- 공개 API는 `/api/v1` 아래에 버전을 유지합니다.
- 요청과 응답은 Pydantic 모델로 명시합니다.
- 새 함수에는 타입 힌트를 사용합니다.
- 금액은 부동소수점 오차를 피할 수 있는 정수 원 단위 또는 `Decimal`을 사용합니다.
- 소득 기준 경계값, 납입 한도, 0원, 최대 한도를 테스트합니다.
- 외부 API 호출은 타임아웃과 실패 처리를 포함합니다.
- 사용자에게 내부 예외, API 키, 원문 스택 트레이스를 노출하지 않습니다.

## 테스트 및 검증

변경 범위에 맞춰 최소한 다음을 확인합니다.

```powershell
python -m compileall app
python -m json.tool app/data/tax_faq.json
git diff --check
```

테스트가 추가된 뒤에는 다음 명령을 기본으로 사용합니다.

```powershell
pytest
```

API 변경 시에는 `/`, `/api/v1/health`, `/docs`와 변경한 엔드포인트를 확인합니다. 프론트엔드 변경 시에는 로딩, 오류, 빈 결과, 모바일 화면을 함께 확인합니다.

## 문서 관리

- 사용자 동작, API, 환경변수, 프로젝트 구조가 바뀌면 README를 함께 갱신합니다.
- 아직 구현되지 않은 기능은 “예정”으로 표시합니다.
- 구현 현황과 GitHub 이슈·PR 표에는 확인 기준일을 적습니다.
- 개인정보, 팀원의 이메일, 비공개 대화 내용, 실제 계좌정보를 문서에 포함하지 않습니다.

## 현재 기준

현재 `main`에는 FastAPI 앱, CORS, 루트·헬스체크 API, `app/data/tax_faq.json`, 연금계좌 세액공제 계산 서비스와 단위 테스트가 있습니다. 진단 API, ChromaDB 검색, LLM 연동, 프론트엔드는 아직 구현 전입니다. 작업 전에는 반드시 최신 파일과 GitHub 상태를 다시 확인합니다.
